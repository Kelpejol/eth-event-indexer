datasource db {
  provider = "sqlite"
  url      = "file:./dev.db"
}

generator client {
  provider = "prisma-client-js"
}

model TransferEvent {
  id        Int      @id @default(autoincrement())
  from      String
  to        String
  value     String
  txHash    String   @unique
  blockNum  Int
  timestamp Int
}
